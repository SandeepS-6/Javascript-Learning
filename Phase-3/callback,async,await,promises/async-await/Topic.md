what is the async and await ?
whenever the promise is created the async and await instead of the then and catch.

async always beside of a function only. await must be beside of promise varible
inside the async function must be write the try and catch as well.

whenever promise created then new function must be create and async must be there.
