what is fetch ?

fetch is basically a js method while using this we will make the http calling thorugh the api's api is nothing just url based on the api we will get the data.

fetch is basically runs on promises only .fetch have the then and catch method must be needed to use.

fetch()
↓
create promise
↓
send network request
↓
wait for response
↓
resolve(response)
